name: tests

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [10.x, 12.x, 14.x]

    steps:
      - run: curl https://johnvansickle.com/ffmpeg/builds/ffmpeg-git-amd64-static.tar.xz -o ffmpeg-git-amd64-static.tar.xz
      - run: tar xf ffmpeg-git-amd64-static.tar.xz
      - run: mkdir $GITHUB_WORKSPACE/.ffmpeg
      - run: cp ffmpeg-*-static/{ffmpeg,ffprobe} $GITHUB_WORKSPACE/.ffmpeg
      - run: rm -rf ffmpeg-*-static
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm ci
      - run: npm run lint
      - run: npm run build
      - name: ${{ env.GITHUB_WORKSPACE }}
        run: npm test
        env:
          FFMPEG_PATH: ${{ env.GITHUB_WORKSPACE }}/.ffmpeg/ffmpeg
          FFPROBE_PATH: ${{ env.GITHUB_WORKSPACE }}/.ffmpeg/ffprobe
